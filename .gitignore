# Runtime data (SQLite WAL must stay in same dir as DB)
data/monitor.db
data/monitor.db-shm
data/monitor.db-wal
*.db
*.db-shm
*.db-wal
browser_profile/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env
.env.server
.env.local

# 本地导出的明文登录 cookie（服务器本地扫码模式，含真实会话凭证）
cookies.json

# Logs
*.log
dashboard.pid

# Test / lint caches
.pytest_cache/
.ruff_cache/

# Inno Setup build output
Output/

# Secrets are never committed: config.py is tracked as a placeholder template
# with env-var defaults. Real keys go into environment variables (.env for
# Docker/systemd) — never commit real keys into config.py.
