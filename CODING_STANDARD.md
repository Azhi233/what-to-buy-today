# 设计规范 —— 新增代码强制标准

> **适用范围**：本仓库所有新增/修改代码（含 AI 生成代码）必须满足本规范。
> **效力**：评审、合并、验收均以此为准；违反即需返工。本文是「强制约束」，不是建议。
> 与本文冲突的实现一律按本文修正；完整生命周期说明见 `DESIGN_SPEC.md`。

---

## 1. 架构与分层

- 分层固定，依赖只能向下：`路由层(app.py) → 服务层(monitor_service) → 领域层(monitor/notifier) → 数据层(database)`。
- 领域层函数保持**纯函数**优先（输入输出明确、无隐藏副作用），便于单测。
- 抓取、过滤、存储、通知、展示**互不耦合**；禁止在抓取层写数据库、在路由层做业务计算。
- 单一入口：常驻入口只有 `app.py`，`run.py` 仅作登录/运维薄壳；**禁止再引入第三套存储或第三套过滤管线**。

## 2. 单一数据源与一致性

- 商品去重只依赖 `items.item_id UNIQUE`，**禁止**新增文件存储（如 seen_items.json）作为第二去重源。
- 数据库路径统一走 `database.resolve_db_path()`，禁止各自用相对 CWD 拼接路径。
- 配置只读自 `config.py`（环境变量覆盖），业务代码内禁止再写死另一套默认值。

## 3. 命名、类型与注释

- 命名：类 `PascalCase`、函数/变量 `snake_case`、常量 `UPPER_SNAKE`、私有 `_` 前缀。
- 公开函数必须有**类型注解**与 docstring（入参 / 返回 / 抛出的异常）。
- 新增魔法数字必须收敛到 `config` 或具名常量并注释理由。

```python
# ❌ BAD
def parse(x):
    if x < 20: ...

# ✅ GOOD
PRICE_DROP_ABS_THRESHOLD = 20  # 降价绝对阈值（元），见 P2-14

def parse_price(text: str) -> float | None:
    """提取价格；无法解析返回 None。"""
```

## 4. 配置与密钥

- 密钥（Bark/SMTP/Telegram/Token）**只允许**通过环境变量或 `.env` 注入，禁止写死在代码或提交进 Git。
- 配置文件只放占位默认值，头注释必须如实说明「是否被 Git 追踪」。
- 新增可调项：提供 `.env.example` 条目 + `config.py` 读取函数（`_env/_env_bool/_env_int`）+ 默认值。
- 外部/用户输入解析（`float()/int()`）必须捕获 `ValueError/TypeError` 并回退默认。

## 5. 数据与持久化

- SQL 一律**参数化**，禁止字符串拼接。
- 建表用 `IF NOT EXISTS`，改结构走 `_migrate()` 幂等 `ALTER`。
- 时间字段统一口径（本地时间），清理与统计用同一基准。
- 共享 SQLite 连接必须持锁访问；写操作走 `_execute()`。

## 6. 安全

- 管理接口走统一鉴权中间件；新增 `/api/*` 路由默认受保护，仅 `/api/healthz` 豁免。
- 鉴权 Token 只走 Header，禁止放在 URL 查询串。
- 所有写接口校验：必填、类型、范围（价格 min≤max 且 max>0、Key 长度等）。
- 前端动态内容一律经 `esc()` 转义后再插入 HTML；敏感字段（如 Bark Key）只回脱敏值。
- 文件名/路径来源不可信时要消毒（如 CSV 导出文件名）。

## 7. 错误处理与容错

- 异常分类处理：**浏览器/上下文崩溃 → 上抛触发重启**；登录失效 → 告警；解析/业务失败 → 记日志继续。
- 禁止 `except Exception: pass` 吞掉关键错误；`search()` 类函数对浏览器崩溃异常必须 `raise`，不得静默返回空。
- 网络/发送类阻塞调用不得卡住事件循环：同步 IO 用 `asyncio.to_thread` 包装。
- 关键循环要有超时与退避，避免死循环或雪崩。

## 8. 异步与并发

- 监控线程自带事件循环，业务协程内**禁止**同步 `requests`/`smtplib`/`time.sleep` 阻塞事件循环。
- 共享可变状态（status/round_items/login_ok）读写要明确线程语义；跨线程只读状态注意可见性。
- 优雅停机：`stop()` 必须等待线程退出（带超时）并释放资源。

## 9. 测试

- 纯算法（价格解析、万元判定、关键词/型号匹配、信用评分、风险识别、IQR、区间校验）**必须**有单元测试。
- 任何修过的回归点都要有对应断言（如「低价小数不被放大为万元」「型号词不匹配被拦」）。
- 测试必须隔离：用临时目录/夹具，禁止触碰生产 `data/monitor.db`。
- 全局状态（如 `service.status`）在测试中显式设置并复位。
- 新增/修改算法同步补测试；`pytest` 必须全绿。

## 10. 日志与可观测

- 关键事件必记：每轮结果（命中/过滤/降价数）、通知成功渠道、浏览器重启、异常。
- 日志不含密钥/Token/cookie 等敏感信息。
- 服务状态取值限定在 `stopped/starting/running/checking/error` 集合内，健康探针语义正确（非 running 即不健康）。

## 11. 依赖与工具链

- 新增第三方依赖需在 `requirements.txt` 注明并给定上下界（`>=x,<y`）；关键依赖锁定版本。
- 新增文件后 `ruff check .` 必须无告警、`compileall` 覆盖到、`Makefile` 无需改清单。
- 删除/重命名任何文件，必须同步更新所有引用与文档，不留失效说明。

## 12. 变更与文档

- 一次提交聚焦一类改动，提交信息带作用域前缀（feat/fix/governance）。
- 改动涉及行为变化时，更新 `README.md` / `DESIGN_SPEC.md` 对应章节。
- 新增配置项/接口/命令，必须在 `.env.example`、README 或部署文档中同步说明。

---

## 强制验收清单（新增代码提交前逐条自检）

- [ ] 分层正确、无循环依赖、无死代码/死导入
- [ ] 公开函数有类型注解 + docstring
- [ ] 密钥走环境变量，配置注释与 Git 追踪状态一致
- [ ] SQL 参数化，时间口径统一，路径统一走 `resolve_db_path()`
- [ ] 写接口有完整校验；前端动态内容转义；敏感字段脱敏
- [ ] 浏览器崩溃异常上抛（不静默吞掉）；同步 IO 已 `to_thread` 包装
- [ ] 纯算法有单测，测试隔离，`pytest` 全绿
- [ ] `ruff check .` 全绿、`compileall` 通过
- [ ] 关键事件有日志，状态机取值合法，healthz 语义正确
- [ ] 文档已同步（README/部署/配置），提交信息规范
