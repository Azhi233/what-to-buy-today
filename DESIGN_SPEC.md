# 闲鱼价格监控 —— 全流程设计规范

> **性质**：本文是「应当满足」的规范清单，覆盖从源码设计到封装落地的全链路，供设计、编码、评审、验收参照。
> **边界**：只列举要求，不含代码修改；不描述具体实现，只定义「环节 + 要求 + 目的/验收点」。
> **适用对象**：本仓库（Python + Playwright + Flask + SQLite + Docker/systemd 的闲鱼价格监控与推送系统）。

---

## 0. 总体原则

| 编号 | 规范要求 | 目的 / 说明 |
|---|---|---|
| P-01 | 单一职责：每个模块只做一类事（抓取 / 过滤 / 存储 / 通知 / 展示分离） | 降低耦合，便于测试与替换 |
| P-02 | 单一入口：对外只有一个常驻入口，CLI 仅作运维薄壳，杜绝「双入口双存储」 | 避免同一系统出现两套行为不一致的管线 |
| P-03 | 数据与代码分离：运行时数据（DB/日志/浏览器 profile）不进版本库 | 可复现、可迁移 |
| P-04 | 配置与代码分离：密钥走环境变量，配置有模板与默认值 | 安全、可部署 |
| P-05 | 只读优先：对目标平台只浏览、不写、不评论、不购买 | 合规与账号安全 |
| P-06 | 可观测：关键路径有日志、有状态、有健康探针 | 无人值守可排查 |

---

## 1. 需求与目标

### 1.1 目标定义
- R-01 明确最终目标：帮助用户「买到价格合适且心仪的产品」，据此定义核心指标（命中率、漏报率、误报率、推送及时性）。
- R-02 明确非功能目标：抗风控、无人值守、多渠道可达、数据可追溯。

### 1.2 需求拆解
- R-03 每个监控项需能表达：关键词、价格区间（min/max）、排除词、必含词、启用状态。
- R-04 明确「可推送商品」的判定口径：价格区间 + 排除词 + 必含词 + 卖家信用 + 风险文案 + 价格异常，六者需有显式规则。
- R-05 明确「不推送但入库」的边界（用于市场分析），与「不推送不入库」要区分。

### 1.3 边界与约束
- R-06 明确平台约束：登录态、频率限制、页面结构可能变化、无头浏览器风控。
- R-07 明确数据约束：价格单位歧义（万元缩写）、标题噪声（猜你喜欢/配件/仿品）。

---

## 2. 架构设计

- A-01 分层清晰：`表示层(Flask 路由) → 服务层(监控服务) → 领域层(抓取/过滤/通知) → 数据层(SQLite)`，依赖只能向下。
- A-02 抓取与业务解耦：`GoofishMonitor` 只负责「取数」，过滤/评估/推送在服务层完成。
- A-03 数据层线程安全：共享 SQLite 连接需加锁 + `check_same_thread=False` + WAL，避免并发写损坏。
- A-04 后台任务独立于 Web 进程/线程：监控循环不得阻塞 HTTP 请求，通知发送不得阻塞监控循环（同步 IO 入线程池）。
- A-05 浏览器生命周期可管理：启动/停止/崩溃自动重启/定时回收，异常分类（浏览器崩溃 vs 业务异常）明确。
- A-06 状态机清晰：服务状态 `stopped/starting/running/checking/error` 取值集合固定，且状态转移可解释。
- A-07 无循环依赖：工具函数（如浏览器错误判定）下沉到被依赖的底层模块。

---

## 3. 源码与编码

- C-01 命名规范统一：类 `PascalCase`、函数/变量 `snake_case`、常量 `UPPER_SNAKE`、私有成员 `_` 前缀。
- C-02 类型注解完整：公开函数签名标注参数与返回类型（含 `Optional`/`Union`）。
- C-03 Docstring：模块/公开函数有中文说明，写清「入参、返回、副作用、抛出的异常」。
- C-04 无死代码：不存在未被调用的函数/导入；删除功能时同步删除对应代码与文档。
- C-05 无魔法数字：阈值（降幅 20 元/5%、IQR 系数、超时、间隔）收敛到配置或具名常量并注释理由。
- C-06 异常处理分级：能恢复的异常就地处理并记录，无法恢复的向上抛；不 `except Exception: pass` 吞掉关键错误。
- C-07 禁止裸 `float()/int()` 解析用户或外部输入：必须捕获 `ValueError/TypeError` 并回退默认值。
- C-08 空值与边界：对外部数据（价格/标题/信用）做非空与类型防御，避免 `None` 参与运算。
- C-09 编码兼容：Windows 控制台强制 UTF-8 重配置，避免 emoji/中文输出乱码。
- C-10 单文件职责规模合理：单模块不宜膨胀为「上帝模块」，核心算法可独立成纯函数以便单测。

---

## 4. 配置管理

- F-01 配置与代码分离：所有可调项进 `config`，不散落在业务代码里写死。
- F-02 敏感信息不入库、不进代码库：Bark Key/SMTP 密码/Telegram Token/DASHBOARD_TOKEN 一律环境变量注入。
- F-03 提供 `.env.example` 与 `config.example`（或等价模板），占位值明确标注 `your_*`。
- F-04 环境变量读取有默认值与类型转换（`_env_bool/_env_int`），非法值回退默认并告警。
- F-05 配置有校验：启动时校验必需项（关键词、max_price>0、min≤max），失败给出可读错误而非运行中崩溃。
- F-06 密钥边界注释准确：被 Git 追踪的配置文件必须明确声明「仅占位、真密钥走环境变量」，不得谎称已忽略。
- F-07 运行参数（host/port）可命令行/环境变量覆盖，且默认值对本地安全（如 host=127.0.0.1）。

---

## 5. 数据存储

- D-01 选型有据：单机小数据量用 SQLite，并启用 WAL + `busy_timeout`。
- D-02 Schema 与索引：唯一约束（`item_id UNIQUE`）、查询字段索引（keyword/price）齐全。
- D-03 迁移可重复执行：`CREATE TABLE IF NOT EXISTS` + 增量 `ALTER` + 回填，幂等。
- D-04 时间一致性：所有时间字段统一口径（本地时间 or UTC），清理/统计用同一基准，避免 8 小时偏差。
- D-05 去重语义唯一：商品去重只依赖数据库唯一键，不引入第二套文件存储。
- D-06 保留策略明确：items/history/checks/notifications 各自保留期可配，并提供手动清理 + VACUUM。
- D-07 并发安全：单连接 + 锁 + `check_same_thread=False`；写入用参数化 SQL。
- D-08 数据可导出：CSV 导出文件名消毒（防路径穿越）、带 BOM 兼容 Excel。
- D-09 数据库路径统一：所有入口解析到同一绝对路径，且自动创建父目录。

---

## 6. 安全

- S-01 API 鉴权：管理接口需 Token（环境变量注入），恒定时间比较；健康探针可豁免。
- S-02 鉴权不在 URL 查询串传递 Token（会泄漏进日志/历史/Referer），仅用 Header。
- S-03 默认安全：未配置 Token 时仅允许本机回环访问，远程一律 401。
- S-04 输入校验：所有写接口校验必填、类型、范围（价格 min≤max、max>0、Key 长度）。
- S-05 SQL 注入防护：一律参数化查询，禁止字符串拼接 SQL。
- S-06 XSS 防护：前端所有外部/动态内容经转义（标题、URL、item_id 等）。
- S-07 敏感信息脱敏：Bark Key 展示用掩码，不返回明文。
- S-08 最小权限：容器/服务以非 root 运行；反向代理启用 HTTPS，管理接口不直接暴露公网。
- S-09 依赖安全：锁定依赖版本，及时更新有漏洞的传递依赖。

---

## 7. 爬虫合规与风控（本项目特有）

- W-01 只读：不评论、不购买、不发布、不自动化下单。
- W-02 频率克制：轮询间隔 ≥ 建议值，随机抖动，关键词间随机延迟，避免固定行为指纹。
- W-03 拟人化：随机 UA/视口、模拟滚动与延迟；默认有头模式（无头易触发风控）。
- W-04 登录态复用：持久化 profile，登录态过期有检测与告警，不反复触发验证。
- W-05 遵守平台条款：明确「仅供学习/个人使用」，不绕过登录、不批量爬取、不侵犯隐私。
- W-06 失败可退避：浏览器崩溃/风控触发指数退避 + 自动重启，避免雪上加霜。

---

## 8. 测试

- T-01 核心纯函数必须有单元测试：价格解析（含万元歧义）、关键词/型号匹配、信用评分、风险文案识别、IQR 过滤、价格区间校验。
- T-02 关键回归必须有断言：如「低价小数不被放大为万元」「型号词不匹配被拦」「诈骗词被过滤」「未知信用按配置处理」。
- T-03 API 测试覆盖鉴权、校验、健康探针、导出消毒等关键路由。
- T-04 测试隔离：不得触碰生产数据库，用临时目录/夹具注入。
- T-05 状态可复现：全局状态（如 service.status）在测试中显式设置并复位。
- T-06 测试进入 CI：`pytest` 失败即阻断合并。
- T-07 测试与实现同步：新增/修改算法时同步补/改测试。

---

## 9. 代码质量与工具链

- Q-01 提供 `pyproject.toml` 统一配置 pytest/ruff。
- Q-02 lint 全绿：`ruff check .` 无告警（E/F/W）。
- Q-03 编译检查：`compileall` 覆盖全部源文件（不硬编码文件清单）。
- Q-04 提供 `Makefile` 聚合 `compile/lint/test/check`，一键自检。
- Q-05 依赖分离：`requirements.txt`（运行）与 `requirements-dev.txt`（测试/lint）分开，版本有上下界。

---

## 10. 错误处理与容错

- H-01 异常分类明确：浏览器/上下文崩溃 vs 登录失效 vs 业务解析失败，分别走「重启/告警/记日志」。
- H-02 重试与退避：浏览器启动失败重试 3 次、崩溃指数退避（5→120s）。
- H-03 超时兜底：页面 goto/wait 设超时，page.close 有超时兜底，避免句柄泄漏。
- H-04 优雅停机：`stop()` 等待线程结束（带超时），关闭 DB，清理 PID 文件。
- H-05 单实例保护：PID 文件 + 进程存活校验，防重复启动抢端口。
- H-06 失败可观测：连续 0 条/登录过期等异常要告警，且同一异常限频（如 6h 内不重复）。

---

## 11. 日志与可观测性

- L-01 统一日志格式与级别，控制台 + 轮转文件（5MB×3）。
- L-02 关键事件必记：每轮检查结果、命中/过滤/降价数量、通知成功渠道、浏览器重启、异常。
- L-03 健康探针：`/api/healthz` 免鉴权，反映 DB 可用 + 服务运行态（stopped/error 应判 unhealthy）。
- L-04 运行状态可查询：当前关键词、上次/下次检查时间、最后错误、轮次统计对外可见。
- L-05 日志不含敏感信息（密钥、Token、完整 cookie）。

---

## 12. 依赖管理

- M-01 直接依赖最小化：不引入不必要的第三方库（如健康探针用可选 import）。
- M-02 版本锁定关键依赖（如 playwright 锁到小版本，与基础镜像版本一致）。
- M-03 依赖有上下界（`>=x,<y`），避免破坏性升级。
- M-04 基础镜像与本地依赖版本一致（Playwright 镜像 tag == requirements 中版本）。

---

## 13. 打包与封装

- K-01 有 `requirements.txt` 并可在全新环境 `pip install -r` 复现。
- K-02 有明确入口：`app.py` 为常驻入口，`run.py` 为运维入口，行为文档化。
- K-03 Dockerfile：基于官方 Playwright 镜像、先 COPY 依赖再 COPY 源码（利用层缓存）、EXPOSE 与运行端口一致、VOLUME 持久化 DB/profile。
- K-04 容器以 `--host 0.0.0.0` 启动（否则映射端口不可达）。
- K-05 容器无显示环境时必须 headless 或内置 Xvfb，否则 Chromium 无法启动。
- K-06 compose：数据卷用目录挂载（不用单文件绑定，避免 WAL 不同步）、healthcheck 命中健康探针、重启策略明确。
- K-07 `.dockerignore` 排除 `.git/venv/.env/缓存/运行时数据`，避免把密钥与体积打进镜像。
- K-08 systemd：`EnvironmentFile`、`Restart=always`、`WorkingDirectory`、非 root 用户、`WantedBy=multi-user.target`。

---

## 14. 部署与运维

- O-01 环境矩阵明确：本地 Windows / Docker Compose / systemd(Linux)，各环境的启动方式与差异写进文档。
- O-02 配置注入：容器/服务用 `.env` 或环境变量注入密钥，不把密钥烘焙进镜像。
- O-03 数据持久化：DB 与 browser_profile 挂载到宿主机目录，升级不丢数据。
- O-04 反向代理与 HTTPS：远程访问经 Nginx/Caddy 并启用 TLS，Token 走 Header。
- O-05 优雅升级/回滚：版本 tag、数据迁移幂等、可 `git pull` 后重建。
- O-06 运维健康检查：提供 healthz 供 Uptime Kuma/k8s liveness 探活。

---

## 15. CI/CD

- I-01 提交/PR 触发流水线：安装依赖 → 编译 → lint → 测试。
- I-02 流水线锁定 Python 版本（如 3.11）与依赖版本。
- I-03 失败即阻断：lint/test 任一失败不得合并。
- I-04 密钥不经 CI 明文：CI 不注入真实推送密钥。

---

## 16. 文档

- U-01 README 覆盖：功能简介、快速开始、配置、登录、运行、部署、目录结构。
- U-02 配置文档：环境变量清单、渠道启用方式、密钥获取方式。
- U-03 部署文档：Docker/systemd/NSSM 三步走 + 安全要求（Token、HTTPS）。
- U-04 变更可追溯：问题/修复清单（如本仓库的 OPTIMIZATION_CHECKLIST.md）与 commit 语义化。
- U-05 文档与实现一致：删除/改名文件后同步更新所有引用，不留失效说明。

---

## 17. 版本与发布

- V-01 语义化版本 + 变更记录（CHANGELOG）。
- V-02 提交信息带作用域前缀（feat/fix/governance 等），一次提交聚焦一类改动。
- V-03 发布前跑完整 `make check`（compile + lint + test）并全绿。

---

## 18. 验收清单（各环节汇总，勾选用）

### 源码设计
- [ ] 分层清晰、无循环依赖、无死代码
- [ ] 类型注解 / docstring / 命名规范达标
- [ ] 异常分级、空值/边界防御、无魔法数字

### 配置
- [ ] 密钥全环境变量、模板+默认值、注释准确、启动校验

### 数据
- [ ] 唯一约束+索引、迁移幂等、时间一致、去重单一、路径统一、保留策略

### 安全
- [ ] 鉴权（Header、恒定时间比较、默认本机）、输入校验、参数化 SQL、XSS 转义、脱敏、最小权限

### 爬虫合规
- [ ] 只读、频率+抖动、拟人化、登录态复用、失败退避、遵守条款

### 测试
- [ ] 核心算法单测、回归断言、API 覆盖、隔离、进 CI

### 质量工具
- [ ] ruff 全绿、compileall 覆盖、Makefile 聚合、依赖分离

### 容错/可观测
- [ ] 异常分类、退避重试、优雅停机、单实例、告警限频、healthz 语义正确

### 封装/部署
- [ ] requirements 可复现、入口清晰、Dockerfile/compose/systemd 正确、host/headless/volume/healthcheck 无误

### CI/CD 与文档
- [ ] 流水线完整、失败阻断、README/部署/配置文档齐全且与实现一致
